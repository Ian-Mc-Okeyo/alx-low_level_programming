#include "main.h"
/**
 * mul - multiplies two integers
 *
 * @a: First input digit
 * @b: second input digit
 * Return: the result after multiplying the two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
